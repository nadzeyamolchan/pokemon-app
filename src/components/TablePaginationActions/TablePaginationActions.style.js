import {makeStyles} from "@material-ui/core";

const tablePaginationActionsStyle = makeStyles({
        paginationButtonsWrapper: {
            display: "flex",
            flexDirection: 'row',
            flexWrap: 'nowrap'
        }
    }
)

export default tablePaginationActionsStyle;